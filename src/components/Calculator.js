import React from 'react'
import { ThemeProvider, createTheme } from "@mui/material/styles";
import { Typography } from "@mui/material"

export const Calculator = () => {
  return (
    <div>
      <Typography>
        calculating your mom
      </Typography>
    </div>
  )
}